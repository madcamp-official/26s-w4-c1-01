# 24시간 이어작업 백로그 (자율)

> 부재 동안 위에서부터 하나씩. 규칙: **가산적·안전**하게만. 변경 후 반드시
> `npm test` + `npm run build` (+ 렌더 변경이면 PNG 판독)으로 검증 → 그린이면 커밋·푸시,
> 레드면 되돌림(revert). 애매하면 건드리지 말고 로그에 질문으로 남긴다.
> 렌더 변경은 **배치·좌표·회전 로직을 절대 건드리지 않는다**(어렵게 잡은 부분). 재질/조명/카메라만.

## 대기 (우선순위 순)

- [x] **1. 창밖 하늘 뷰** ✅ — 창 유리를 수직 하늘 그라데이션으로. window_sky_mat()+프리셋별 sky색. night 밤하늘 뚜렷, day 은은, sunset 웜. (2026-07-25)
- [x] **2. 자동배치 개구부 클리어런스** ✅ — openingZones()+generateLayouts/validateCandidates에 openings 지원. 테스트 26/26. (2026-07-25)
- [ ] **3. 카탈로그 확장** — 로컬 GLB 169개 중 카탈로그 미등록분을 glb_audit로 검수 후 양호한 것만 abo3d.js에 추가.
      검증: 감사 통과 + build. (치수는 감사 실측값 사용)
- [ ] **4. 벽/천장 미세 질감** — 벽에 아주 옅은 러프니스 변주(플랫티 제거). 과하지 않게.
      검증: 렌더 판독(전 프리셋 자연스러운지).
- [ ] **5. 몰입캠프 데모 대본** — docs/데모-대본.md. 3분 시연 흐름(방 입력→가구→자동배치→3D→렌더 4종).
      검증: 문서만(안전).
- [ ] **6. 렌더 다각도 옵션** — 같은 배치를 와이드/아늑 2컷으로. 기본 폴백 카메라 개선 포함.
      검증: 렌더 판독.
- [ ] **7. 프리셋 미세조정** — 아침/한낮 차이를 조금 더 또렷하게(눈검수 기반).
      검증: 렌더 판독.

## 완료

- (첫 세션: 계획서, 렌더 대개조+프리셋UI, 자동배치 동선+테스트, GLB감사, sunset 골든아워, 갤러리 — docs/24h-작업로그.md 참고)
