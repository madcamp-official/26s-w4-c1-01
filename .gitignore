# node
web/node_modules/
web/dist/
*.log

# android — SDK 경로(기기마다 다름)와 빌드 산출물
android/local.properties
android/.gradle/
android/build/
android/app/build/

# python
server/__pycache__/
server/app/__pycache__/
server/.venv/
**/__pycache__/
server/app/community.db

# secrets
.env
server/.env

# SSH 키 — 절대 커밋 금지
*.pem
camp3_*

# 폰에 바로 설치할 수 있게 빌드해 둔 APK — 바이너리라 커밋하지 않는다
web/public/*.apk

# 저장된 배치 렌더 PNG(사용자 데이터) — 커밋 금지
server/app/uploads/

# 날짜 붙인 APK 사본(캐시 우회용) — 바이너리라 커밋하지 않는다
web/public/bangkku-*.apk
